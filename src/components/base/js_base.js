module.exports = () => {
    let bases = $('.base')
    if (bases.length) {
        for (let base of bases) {
            let $base = $(base)
            // do some jquery things with base component
        }
    }
}
